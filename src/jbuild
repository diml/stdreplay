(jbuild_version 1)

(library
 ((name forkpty)
  (libraries (unix))
  (c_names (stubs))
  (c_library_flags (:include flags.sexp))
  ))

(rule
 (with-stdout-to ocaml-conf
  (run ${OCAMLC} -config)))

(rule
 (with-stdout-to flags.sexp
  (run ${OCAML} ${path:config/gen.ml} ${path:ocaml-conf})))

